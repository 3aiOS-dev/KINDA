//
// StoreAppDetailsView.swift
// KINDA
//
// صفحة التفاصيل لم تعد مستخدمة في الواجهة.
// هذا الملف يبقى كـ compatibility stub حتى لا يفشل Target
// إذا بقي الملف مضافًا إلى Xcode.
//

import SwiftUI

struct StoreAppDetailsView: View {
    init<T>(app: T) {
        // Compatibility initializer only.
    }

    init() {}

    var body: some View {
        EmptyView()
    }
}
